"use client";
import {useEffect,useState} from "react";
import {createClient} from "@/lib/supabase/client";
type Match={id:string;match_number:number;status:string;participant_a_id:string|null;participant_b_id:string|null;category_id:string;winner_id:string|null};
export default function RunningClient({tournamentId,initialMatches}:{tournamentId:string;initialMatches:Match[]}){const[matches,setMatches]=useState(initialMatches);const[busy,setBusy]=useState<string|null>(null);const s=createClient();useEffect(()=>{const ch=s.channel(`matches-${tournamentId}`).on("postgres_changes",{event:"*",schema:"public",table:"matches",filter:`tournament_id=eq.${tournamentId}`},payload=>{if(payload.eventType==="DELETE")setMatches(x=>x.filter(m=>m.id!==payload.old.id));else{const n=payload.new as Match;setMatches(x=>x.some(m=>m.id===n.id)?x.map(m=>m.id===n.id?n:m):[...x,n].sort((a,b)=>a.match_number-b.match_number));}}).subscribe();return()=>{void s.removeChannel(ch)}},[tournamentId]);async function win(m:Match,id:string){setBusy(m.id);const{error}=await s.rpc("record_match_winner",{p_match_id:m.id,p_winner_id:id});if(error)alert(error.message);setBusy(null)}return <section className="running-list">{matches.length===0?<div className="empty-state">Боёв пока нет.</div>:matches.map(m=><article className="running-card" key={m.id}><div className="eyebrow">БОЙ #{m.match_number} · {m.status}</div><div className="running-actions"><button disabled={!!m.winner_id||busy===m.id||!m.participant_a_id} onClick={()=>m.participant_a_id&&win(m,m.participant_a_id)}>ПОБЕДА A</button><button disabled={!!m.winner_id||busy===m.id||!m.participant_b_id} onClick={()=>m.participant_b_id&&win(m,m.participant_b_id)}>ПОБЕДА B</button></div>{m.winner_id&&<p className="muted">Результат сохранён.</p>}</article>)}</section>}
